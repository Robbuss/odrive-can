{
    "private": true,
    "scripts": {
        "dev": "docker compose up --build backend frontend",
        "up": "docker compose up -d backend frontend db pgadmin",
        "up:editor": "docker compose up -d editor",
        "restart": "docker compose restart backend frontend",
        "rebuild": "docker compose up -d --build backend frontend",
        "stop": "docker compose stop backend frontend",
        "logs": "docker compose logs -f backend frontend",
        "logs:backend": "docker compose logs -f backend",
        "logs:frontend": "docker compose logs -f frontend",
        "logs:db": "docker compose logs -f db",
        "down:all": "docker compose down"
      }
}
